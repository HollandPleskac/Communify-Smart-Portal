class Goal {
  name: string
  startDate: string
  estimatedFinishDate: string

  constructor(name: string, startDate: string, estimatedFinishDate: string) {
    this.name = name
    this.startDate = startDate
    this.estimatedFinishDate = estimatedFinishDate
  }
}

export default Goal
